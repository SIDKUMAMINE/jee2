spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery: SERVICE-CLIENT
  application:
    name: SERVICE-CLIENT
  datasource:
    url: jdbc:h2:mem:test
#    username: root
#    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
      port: 8088